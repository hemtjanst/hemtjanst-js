import {Client, Server} from "./hemtjanst"
import {Device, DeviceType} from "./device"
import {FeatureType} from "./feature";

export = {
    client: Client,
    server: Server,
    device: Device,
    deviceType: DeviceType,
    featureType: FeatureType,
}